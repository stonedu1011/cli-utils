# OpenSSL node configuration file
[ req ]
prompt=no
distinguished_name = distinguished_name
req_extensions = extensions

[ distinguished_name ]
commonName = redis

[ extensions ]
subjectAltName = critical,DNS:localhost,DNS:redis,IP:127.0.0.1
